{
  "name": "flowspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node ./scripts/dev-with-port-guard.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test:audio-weather": "rm -rf .tmp-tests && npx tsc --outDir .tmp-tests --module ESNext --target ES2020 --moduleResolution bundler --lib ES2020,DOM src/audio/AudioManager.ts src/audio/weatherAudioConfig.ts && node --experimental-specifier-resolution=node --test tests/audioManager.test.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/three": "^0.185.0",
    "typescript": "^5.2.2",
    "vite": "^5.3.1"
  }
}
